export const renderResetPassword = (req, res) => {
  res.render("resetpassword/reset-password");
};
export const renderEmailSent = (req, res) => {
  res.render("resetpassword/reset-password-sent");
};
export const renderResetPasswordForm = (req, res) => {
  res.render("resetpassword/reset-password-form");
};
